/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java Library project to get you started.
 * For more details take a look at the Java Libraries chapter in the Gradle
 * User Manual available at https://docs.gradle.org/6.1.1/userguide/java_library_plugin.html
 */

plugins{
	id 'application'
}

apply from: 'buildscript/run.gradle'
apply from: 'buildscript/clean.gradle'

version '1.0-SNAPSHOT'

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}

repositories {
    // Use jcenter for resolving dependencies.
    // You can declare any Maven/Ivy/file repository here.
    jcenter()
}

dependencies {
	implementation  'org.apache.tomcat:tomcat-catalina:latest.release'
    // This dependency is used internally, and not exposed to consumers on their own compile classpath.
    testImplementation 'org.testng:testng:7.0.0'
}

test {
    // Use TestNG for unit tests
    useTestNG(){ suites 'src/test/resources/testng.xml' }
}

javadoc {
    options.addStringOption('encoding', 'UTF-8')
}
